#introduction: Understanding Data types 
#Generate Data
1:10
n=c(1,3,4,9)
n
c(1,'a',4,9)
#Assign variable name to the value
x=10; x<-10; x->10;
x
#To combine numeric values into a vector
c(1,2,5)
#Arithmetic Operations of vectors are 

a+b
d=c(1,2,3,4)


d=c(3,4)
e=c(5,6,7,8,9)
d+e
#subtraction
a-b

#character object is used to represent
x=5.2
x=as.character(5.2)#error is here because char and int are different data types
x+5
#concatenation of strings
paste("hey", "there")
#data importing
#cslv, xls, xlsx, sav, dta, por, sas
#importing data from Text and csv files
read.csv(file.choose(file.csv))
